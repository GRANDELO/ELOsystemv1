/// <reference types="node" />
import { Readable, ReadableOptions } from 'stream';
import IReadableMock from './IReadableMock';
export default class BufferReadableMock extends Readable implements IReadableMock {
    it: IterableIterator<any>;
    encoding: BufferEncoding;
    constructor(source: Iterable<any> | ArrayLike<any>, options?: ReadableOptions);
    _read(): void;
}
